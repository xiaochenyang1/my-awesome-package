# 可拖拽流程编排组件设计文档 🎨

## 📦 包名建议
- `flow-node-editor`
- `react-flow-steps`
- `draggable-flow-builder`
- `@yourname/flow-editor`

---

## 🎯 为什么这个想法很棒？

### 复用率分析
| 使用场景 | 复用率 | 市场需求 |
|---------|--------|---------|
| 审批流程设计 | ⭐⭐⭐⭐⭐ | 极高 |
| 工作流编排 | ⭐⭐⭐⭐⭐ | 极高 |
| 步骤引导/向导 | ⭐⭐⭐⭐ | 高 |
| 数据处理流程 | ⭐⭐⭐⭐ | 高 |
| 任务流程管理 | ⭐⭐⭐⭐ | 高 |
| 表单多步骤 | ⭐⭐⭐⭐ | 高 |

### 核心优势
- ✅ **差异化强**：比普通工具库更有亮点
- ✅ **可视化**：解决实际业务痛点（流程配置）
- ✅ **高需求**：很多项目需要流程编排功能
- ✅ **技术挑战适中**：不太简单也不太难
- ✅ **扩展性好**：后续可以做成低代码平台的一部分

---

## 🎨 产品设计

### 基础版 - 线性流程（你说的样式）

```
┌─────────┐      ┌─────────┐      ┌─────────┐      ┌─────────┐
│  节点1  │─────▶│  节点2  │─────▶│  节点3  │─────▶│  节点4  │
│ 发起申请 │      │ 部门审批 │      │ 财务审批 │      │   完成  │
└─────────┘      └─────────┘      └─────────┘      └─────────┘
    ↕️               ↕️               ↕️               ↕️
  可拖拽          可拖拽          可拖拽          可拖拽
```

**特点：**
- 节点横向排列
- 箭头自动连接
- 拖拽改变顺序
- 每个节点可编辑内容

### 进阶版 - 支持分支（可选）

```
                  ┌─────────┐
            ┌────▶│ 审批通过 │────┐
┌─────────┐ │     └─────────┘    │     ┌─────────┐
│ 提交申请 │─┤                    ├────▶│   完成  │
└─────────┘ │     ┌─────────┐    │     └─────────┘
            └────▶│ 审批驳回 │────┘
                  └─────────┘
```

---

## 🛠️ 功能详细设计

### 核心功能列表

#### 1️⃣ 节点管理

##### 1.1 节点结构
```typescript
interface FlowNode {
  id: string;              // 节点唯一ID
  order: number;           // 节点顺序（用于排序）
  title: string;           // 节点标题
  description?: string;    // 节点描述
  type?: string;           // 节点类型（start/process/end/condition）
  status?: 'pending' | 'active' | 'completed' | 'error';  // 节点状态
  data?: Record<string, any>;  // 节点自定义数据
  editable?: boolean;      // 是否可编辑
  deletable?: boolean;     // 是否可删除
}
```

##### 1.2 节点操作
- ✅ **添加节点**：在指定位置插入新节点
- ✅ **删除节点**：移除节点并重新连接
- ✅ **编辑节点**：修改节点内容
- ✅ **拖拽排序**：改变节点顺序
- ✅ **复制节点**：快速创建相似节点

---

#### 2️⃣ 拖拽功能

##### 2.1 拖拽特性
```typescript
interface DragOptions {
  enabled?: boolean;           // 是否启用拖拽，默认 true
  animation?: number;          // 动画时长（毫秒），默认 300
  ghostClass?: string;         // 拖拽时的样式类名
  chosenClass?: string;        // 选中时的样式类名
  dragClass?: string;          // 正在拖拽的样式类名
  handle?: string;             // 拖拽手柄选择器（只能通过手柄拖拽）
  onDragStart?: (node: FlowNode) => void;    // 拖拽开始回调
  onDragEnd?: (node: FlowNode) => void;      // 拖拽结束回调
  onDragMove?: (from: number, to: number) => boolean;  // 拖拽移动回调，返回 false 阻止
}
```

##### 2.2 拖拽实现方案
- **方案 A**：使用 `react-beautiful-dnd`（推荐）
  - ✅ 动画流畅
  - ✅ 无障碍支持
  - ✅ 触摸屏支持

- **方案 B**：使用 `dnd-kit`
  - ✅ 更轻量
  - ✅ 更灵活

- **方案 C**：使用 HTML5 原生 Drag API
  - ✅ 零依赖
  - ❌ 需要自己处理动画

---

#### 3️⃣ 连线功能

##### 3.1 连线样式
```typescript
interface LineStyle {
  type: 'solid' | 'dashed' | 'dotted';  // 线条类型
  color?: string;          // 线条颜色，默认 #1890ff
  width?: number;          // 线条宽度，默认 2
  arrow?: boolean;         // 是否显示箭头，默认 true
  arrowSize?: number;      // 箭头大小，默认 8
  animated?: boolean;      // 是否显示动画（流动效果）
  label?: string;          // 连线上的文字
}
```

##### 3.2 连线规则
- 节点之间自动连线
- 支持直线、折线、曲线
- 支持动画效果（蚂蚁线）

---

#### 4️⃣ 节点编辑

##### 4.1 内联编辑
```typescript
interface EditConfig {
  mode: 'inline' | 'modal' | 'drawer';  // 编辑模式
  fields?: Array<{
    name: string;          // 字段名
    label: string;         // 字段标签
    type: 'input' | 'textarea' | 'select' | 'date' | 'custom';
    required?: boolean;    // 是否必填
    placeholder?: string;  // 占位符
    options?: Array<{ label: string; value: any }>;  // 下拉选项
    render?: (value: any, onChange: (val: any) => void) => React.ReactNode;  // 自定义渲染
  }>;
  onSave?: (node: FlowNode) => Promise<boolean | void>;  // 保存回调
  onCancel?: () => void;   // 取消回调
}
```

##### 4.2 编辑方式
- **双击编辑**：双击节点进入编辑模式
- **右键菜单**：右键显示操作菜单
- **工具栏**：选中节点显示工具栏

---

#### 5️⃣ 布局方式

##### 5.1 横向布局（默认）
```
节点1 → 节点2 → 节点3 → 节点4
```

##### 5.2 纵向布局
```
节点1
  ↓
节点2
  ↓
节点3
  ↓
节点4
```

##### 5.3 自由布局（进阶）
```
用户可以自由拖拽节点到任意位置
支持画布缩放、平移
```

---

## 💻 API 设计

### React 组件版本

#### 基础用法
```tsx
import FlowEditor from 'flow-node-editor';

function App() {
  const [nodes, setNodes] = useState<FlowNode[]>([
    { id: '1', order: 1, title: '发起申请', type: 'start' },
    { id: '2', order: 2, title: '部门审批', type: 'process' },
    { id: '3', order: 3, title: '财务审批', type: 'process' },
    { id: '4', order: 4, title: '完成', type: 'end' }
  ]);

  return (
    <FlowEditor
      nodes={nodes}
      onChange={setNodes}
      onNodeClick={(node) => console.log('点击节点:', node)}
      onNodeAdd={(index) => console.log('在位置', index, '添加节点')}
      onNodeDelete={(node) => console.log('删除节点:', node)}
    />
  );
}
```

#### 完整配置
```tsx
<FlowEditor
  // 数据
  nodes={nodes}                    // 节点数据
  onChange={setNodes}              // 数据变化回调

  // 布局
  layout="horizontal"              // 布局方向: horizontal | vertical | free
  nodeSpacing={100}                // 节点间距（像素）

  // 拖拽
  draggable={true}                 // 是否可拖拽
  dragHandle=".drag-handle"        // 拖拽手柄

  // 连线
  lineStyle={{
    type: 'solid',
    color: '#1890ff',
    animated: true
  }}

  // 编辑
  editable={true}                  // 是否可编辑
  editMode="modal"                 // 编辑模式
  editFields={[                    // 编辑字段配置
    { name: 'title', label: '标题', type: 'input', required: true },
    { name: 'description', label: '描述', type: 'textarea' }
  ]}

  // 工具栏
  showToolbar={true}               // 是否显示工具栏
  toolbarPosition="top"            // 工具栏位置: top | bottom | left | right

  // 样式
  className="custom-flow"
  nodeClassName="custom-node"

  // 事件
  onNodeClick={(node) => {}}       // 节点点击
  onNodeDoubleClick={(node) => {}} // 节点双击
  onNodeAdd={(index) => {}}        // 添加节点
  onNodeDelete={(node) => {}}      // 删除节点
  onNodeEdit={(node) => {}}        // 编辑节点
  onDragStart={(node) => {}}       // 拖拽开始
  onDragEnd={(from, to) => {}}     // 拖拽结束

  // 自定义渲染
  renderNode={(node) => (          // 自定义节点渲染
    <div>{node.title}</div>
  )}
  renderLine={(from, to) => (      // 自定义连线渲染
    <div>from {from.id} to {to.id}</div>
  )}
  renderToolbar={() => (           // 自定义工具栏
    <div>自定义工具栏</div>
  )}
/>
```

---

## 🎨 样式设计

### 节点样式

#### 默认样式
```css
.flow-node {
  /* 基础样式 */
  width: 200px;
  min-height: 80px;
  padding: 16px;
  background: #fff;
  border: 2px solid #d9d9d9;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);

  /* 鼠标效果 */
  cursor: move;
  transition: all 0.3s;
}

.flow-node:hover {
  border-color: #1890ff;
  box-shadow: 0 4px 12px rgba(24, 144, 255, 0.2);
}

.flow-node.dragging {
  opacity: 0.5;
  transform: rotate(5deg);
}
```

#### 不同类型节点
```css
/* 开始节点 */
.flow-node-start {
  border-color: #52c41a;
  background: linear-gradient(135deg, #f6ffed 0%, #d9f7be 100%);
}

/* 处理节点 */
.flow-node-process {
  border-color: #1890ff;
  background: linear-gradient(135deg, #e6f7ff 0%, #bae7ff 100%);
}

/* 结束节点 */
.flow-node-end {
  border-color: #ff4d4f;
  background: linear-gradient(135deg, #fff1f0 0%, #ffccc7 100%);
}

/* 条件节点（菱形） */
.flow-node-condition {
  width: 100px;
  height: 100px;
  border-radius: 0;
  transform: rotate(45deg);
  border-color: #faad14;
}
```

### 连线样式
```css
.flow-line {
  stroke: #1890ff;
  stroke-width: 2;
  fill: none;
}

.flow-line-arrow {
  fill: #1890ff;
}

/* 动画效果 */
.flow-line-animated {
  stroke-dasharray: 5;
  animation: flow-line-dash 1s linear infinite;
}

@keyframes flow-line-dash {
  to {
    stroke-dashoffset: -10;
  }
}
```

---

## 🏗️ 项目结构

```
flow-node-editor/
├── src/
│   ├── index.tsx                    # 主入口
│   ├── FlowEditor.tsx               # 主组件
│   ├── components/
│   │   ├── FlowNode.tsx            # 节点组件
│   │   ├── FlowLine.tsx            # 连线组件
│   │   ├── FlowToolbar.tsx         # 工具栏组件
│   │   ├── NodeEditor.tsx          # 节点编辑器
│   │   └── ContextMenu.tsx         # 右键菜单
│   ├── hooks/
│   │   ├── useDraggable.ts         # 拖拽 Hook
│   │   ├── useFlowData.ts          # 数据管理 Hook
│   │   └── useNodeEdit.ts          # 编辑 Hook
│   ├── utils/
│   │   ├── layout.ts               # 布局计算
│   │   ├── line.ts                 # 连线计算
│   │   └── validate.ts             # 数据验证
│   ├── types/
│   │   └── index.ts                # TypeScript 类型定义
│   └── styles/
│       ├── index.css               # 样式入口
│       ├── node.css                # 节点样式
│       ├── line.css                # 连线样式
│       └── toolbar.css             # 工具栏样式
├── examples/                        # 示例
│   ├── basic.tsx                   # 基础示例
│   ├── custom-node.tsx             # 自定义节点
│   ├── approval-flow.tsx           # 审批流程
│   └── workflow.tsx                # 工作流
├── test/                           # 测试
│   ├── FlowEditor.test.tsx
│   └── hooks.test.ts
├── dist/                           # 构建输出
├── package.json
├── tsconfig.json
├── rollup.config.js
├── README.md
└── CHANGELOG.md
```

---

## 📖 使用场景示例

### 场景 1：审批流程配置
```tsx
import FlowEditor from 'flow-node-editor';

function ApprovalFlowConfig() {
  const [nodes, setNodes] = useState([
    { id: '1', order: 1, title: '发起申请', type: 'start', data: { approver: '' } },
    { id: '2', order: 2, title: '部门经理审批', type: 'process', data: { approver: 'dept_manager' } },
    { id: '3', order: 3, title: '总经理审批', type: 'process', data: { approver: 'general_manager' } },
    { id: '4', order: 4, title: '财务审批', type: 'process', data: { approver: 'finance' } },
    { id: '5', order: 5, title: '完成', type: 'end', data: {} }
  ]);

  return (
    <FlowEditor
      nodes={nodes}
      onChange={setNodes}
      editFields={[
        { name: 'title', label: '节点名称', type: 'input', required: true },
        {
          name: 'data.approver',
          label: '审批人',
          type: 'select',
          options: [
            { label: '部门经理', value: 'dept_manager' },
            { label: '总经理', value: 'general_manager' },
            { label: '财务', value: 'finance' }
          ]
        }
      ]}
      onNodeAdd={(index) => {
        const newNode = {
          id: Date.now().toString(),
          order: index + 1,
          title: '新审批节点',
          type: 'process',
          data: { approver: '' }
        };
        const newNodes = [...nodes];
        newNodes.splice(index, 0, newNode);
        setNodes(newNodes.map((n, i) => ({ ...n, order: i + 1 })));
      }}
    />
  );
}
```

### 场景 2：多步骤表单向导
```tsx
function StepWizard() {
  const [nodes, setNodes] = useState([
    { id: '1', order: 1, title: '基本信息', status: 'completed' },
    { id: '2', order: 2, title: '详细信息', status: 'active' },
    { id: '3', order: 3, title: '确认提交', status: 'pending' }
  ]);

  const [currentStep, setCurrentStep] = useState(1);

  return (
    <div>
      <FlowEditor
        nodes={nodes}
        onChange={setNodes}
        draggable={false}  // 步骤向导不需要拖拽
        onNodeClick={(node) => setCurrentStep(node.order - 1)}
        renderNode={(node) => (
          <div className={`step-node step-${node.status}`}>
            <div className="step-number">{node.order}</div>
            <div className="step-title">{node.title}</div>
          </div>
        )}
      />

      {/* 步骤内容 */}
      <div className="step-content">
        {currentStep === 0 && <BasicInfoForm />}
        {currentStep === 1 && <DetailInfoForm />}
        {currentStep === 2 && <ConfirmForm />}
      </div>
    </div>
  );
}
```

### 场景 3：数据处理流程
```tsx
function DataPipeline() {
  const [nodes, setNodes] = useState([
    { id: '1', order: 1, title: '数据源', type: 'start', data: { source: 'mysql' } },
    { id: '2', order: 2, title: '数据清洗', type: 'process', data: { operation: 'clean' } },
    { id: '3', order: 3, title: '数据转换', type: 'process', data: { operation: 'transform' } },
    { id: '4', order: 4, title: '数据加载', type: 'end', data: { target: 'warehouse' } }
  ]);

  return (
    <FlowEditor
      nodes={nodes}
      onChange={setNodes}
      renderNode={(node) => (
        <div className="pipeline-node">
          <div className="node-icon">
            {node.type === 'start' && '📥'}
            {node.type === 'process' && '⚙️'}
            {node.type === 'end' && '📤'}
          </div>
          <div className="node-title">{node.title}</div>
          <div className="node-info">
            {node.data.source && `来源: ${node.data.source}`}
            {node.data.operation && `操作: ${node.data.operation}`}
            {node.data.target && `目标: ${node.data.target}`}
          </div>
        </div>
      )}
    />
  );
}
```

---

## 🚀 开发计划

### 第一版（MVP）- 基础功能
**预计开发时间：5-7 天**

- ✅ 基础节点显示（横向布局）
- ✅ 节点拖拽排序（使用 react-beautiful-dnd）
- ✅ 自动连线（SVG 直线 + 箭头）
- ✅ 添加/删除节点
- ✅ 节点内联编辑（双击编辑标题）
- ✅ 基础样式（3 种节点类型）
- ✅ TypeScript 类型定义

**技术栈：**
- React 18
- TypeScript
- react-beautiful-dnd
- styled-components 或 emotion

### 第二版 - 增强功能
**预计开发时间：5-7 天**

- ✅ 弹窗编辑（支持多字段）
- ✅ 自定义编辑字段配置
- ✅ 工具栏（添加、删除、撤销、重做）
- ✅ 右键菜单
- ✅ 节点状态（pending/active/completed/error）
- ✅ 连线动画效果
- ✅ 纵向布局支持
- ✅ 节点复制功能

### 第三版 - 高级功能
**预计开发时间：7-10 天**

- ✅ 自由布局（画布缩放、平移）
- ✅ 条件分支（支持 if/else）
- ✅ 并行节点
- ✅ 节点分组
- ✅ 数据导入/导出（JSON）
- ✅ 撤销/重做
- ✅ 小地图（缩略图）
- ✅ 快捷键支持

---

## 📊 技术选型对比

### 拖拽库选择

| 库名 | 优势 | 劣势 | 推荐指数 |
|------|------|------|---------|
| react-beautiful-dnd | 动画流畅，体验好 | 只支持单轴拖拽 | ⭐⭐⭐⭐⭐ |
| dnd-kit | 轻量，灵活，支持多场景 | 需要更多配置 | ⭐⭐⭐⭐ |
| react-dnd | 功能强大 | 较重，学习成本高 | ⭐⭐⭐ |
| 原生 HTML5 DnD | 零依赖 | 需要自己实现很多功能 | ⭐⭐ |

**建议：第一版用 react-beautiful-dnd，后续如需自由布局再换 dnd-kit**

### 连线方案

| 方案 | 优势 | 劣势 | 推荐指数 |
|------|------|------|---------|
| SVG | 性能好，浏览器原生支持 | 需要计算坐标 | ⭐⭐⭐⭐⭐ |
| Canvas | 性能最好 | 交互实现复杂 | ⭐⭐⭐ |
| CSS + Div | 简单 | 只能直线，不够灵活 | ⭐⭐ |

**建议：使用 SVG**

---

## 💰 市场分析

### 竞品分析

| 产品 | 类型 | 价格 | 功能 | 缺点 |
|------|------|------|------|------|
| G6（AntV） | 图可视化库 | 免费 | 功能强大 | 学习成本高，偏底层 |
| LogicFlow | 流程图编辑框架 | 免费 | 专注流程图 | 定制成本高 |
| React Flow | React流程图库 | 免费/付费 | 生态好 | 配置复杂 |
| **你的产品** | 简化版流程编排 | 免费 | **简单易用** | **功能相对简单** |

### 差异化优势
- ✅ **极简 API**：5 分钟上手
- ✅ **开箱即用**：默认样式美观
- ✅ **专注线性流程**：不做复杂图形
- ✅ **轻量级**：包体积 < 50KB

---

## 📦 包大小预估

| 模块 | 预估大小 | 说明 |
|------|---------|------|
| 核心逻辑 | ~10KB | 节点管理、数据处理 |
| 拖拽功能 | ~15KB | react-beautiful-dnd |
| 样式 | ~5KB | CSS |
| SVG 连线 | ~3KB | 连线绘制 |
| **总计** | **~33KB** | **gzip 后约 10KB** |

---

## ✅ 成功标准

### MVP 版本成功指标
- ✅ 能拖拽排序节点
- ✅ 能添加/删除节点
- ✅ 能编辑节点内容
- ✅ 连线自动更新
- ✅ 提供完整 TypeScript 类型
- ✅ 有 3 个以上使用示例
- ✅ 测试覆盖率 > 80%

---

## 🎯 后续扩展方向

### 短期（3 个月内）
1. **Vue 版本**：`@yourname/flow-editor-vue`
2. **主题系统**：支持自定义主题
3. **更多节点类型**：循环、并行、等待等

### 中期（6 个月内）
1. **低代码平台**：结合表单设计器
2. **模板市场**：预设常用流程模板
3. **导入导出**：支持 BPMN 格式

### 长期（1 年内）
1. **可视化 BI**：数据处理流程
2. **AI 助手**：自动生成流程
3. **协作功能**：多人实时编辑

---

## 📝 总结

### 为什么推荐做这个？

#### 1. **市场需求大** ✅
- 审批流程、工作流配置是企业软件的刚需
- 很多项目需要流程编排但不想引入重量级库
- 简化版的流程编排工具市场空白

#### 2. **技术难度适中** ✅
- 不像纯工具函数那么简单（有挑战）
- 也不像完整的流程引擎那么复杂
- 适合作为进阶项目

#### 3. **差异化明显** ✅
- 比普通工具库更有亮点
- 可视化组件更容易展示
- 适合做 Demo 演示

#### 4. **可持续发展** ✅
- 可以逐步扩展功能
- 可以做成产品矩阵（React/Vue/Angular）
- 可以发展成低代码平台

#### 5. **简历加分** ✅
- 展示组件设计能力
- 展示拖拽交互实现
- 展示可视化开发经验

---

## 🎯 下一步建议

### 方案 A：先做工具库，再做流程编排
**优势：** 快速成功，降低风险
1. 先做格式化工具库（2-3 天完成）
2. 发布第一个 npm 包，积累经验
3. 再做流程编排组件（1-2 周完成）

### 方案 B：直接做流程编排（推荐）
**优势：** 一步到位，差异化强
1. 专注做流程编排组件
2. 第一版 MVP（5-7 天）
3. 快速迭代，逐步完善

### 我的建议
**如果你时间充足 + 想做亮点项目**：选方案 B
**如果你想快速出成果**：选方案 A

---

**你觉得怎么样？** 🤔

如果你喜欢这个方向，我可以帮你：
1. 初始化 React 项目
2. 搭建基础架构
3. 实现第一版 MVP（拖拽 + 编辑 + 连线）
4. 提供完整的代码实现

随时告诉我你的想法！😊
