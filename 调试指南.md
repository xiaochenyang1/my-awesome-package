# 调试指南 🛠️

## 项目已经完成，可以开始调试了！✅

---

## 快速开始 🚀

### 方式 1：一键启动（推荐）

```bash
# 进入示例项目
cd examples/react-example

# 启动开发服务器
pnpm dev
```

然后访问：**http://localhost:3000** 🌐

---

## 详细调试步骤 📋

### 1. 检查构建产物

```bash
# 查看核心包构建结果
ls -la packages/core/dist

# 查看 React 包构建结果
ls -la packages/react/dist
```

**应该看到：**
- `index.js` - CommonJS 格式
- `index.d.ts` - TypeScript 类型定义
- `esm/` - ES Module 格式（如果有）

---

### 2. 启动开发服务器

```bash
cd examples/react-example
pnpm dev
```

**应该看到：**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

---

### 3. 浏览器访问

打开浏览器访问：`http://localhost:3000`

**应该看到：**
- ✅ 页面标题："流程设计器示例"
- ✅ 流程信息：流程名称、版本、节点数量
- ✅ 节点列表
- ✅ 架构说明

---

## 调试技巧 🔍

### 1. 查看控制台输出

打开浏览器开发者工具（F12），查看 Console 面板：

```javascript
// 应该没有错误
// 可以看到配置数据
console.log('流程配置:', config);
```

### 2. 修改配置文件

打开 `examples/react-example/src/App.tsx`，找到配置部分：

```typescript
const data: FlowConfig = {
  id: 'demo-flow',
  name: '演示流程',  // 修改这里试试
  version: '1.0.0',
  // ...
};
```

保存文件后，页面会自动刷新（热更新）。

### 3. 测试动态表单

在配置中添加一个新字段：

```typescript
{
  name: 'testField',
  label: '测试字段',
  type: 'input',
  required: true,
  placeholder: '这是一个测试字段'
}
```

保存后查看页面是否显示新字段。

### 4. 测试条件显示

修改字段的 `visible` 属性：

```typescript
{
  name: 'reason',
  label: '请假原因',
  type: 'textarea',
  visible: 'type === "leave"',  // 条件显示
  required: true
}
```

选择不同的申请类型，查看字段是否动态显示/隐藏。

---

## 常见问题排查 ❓

### 问题 1：页面加载失败

**检查：**
```bash
# 确认依赖已安装
pnpm install

# 确认包已构建
ls packages/core/dist
ls packages/react/dist
```

**解决：**
```bash
# 重新构建
cd packages/core && pnpm build
cd ../react && pnpm build
```

---

### 问题 2：TypeScript 错误

**检查：**
```bash
# 查看 TypeScript 错误
pnpm build
```

**解决：**
- 检查类型定义是否正确
- 查看 `tsconfig.json` 配置

---

### 问题 3：热更新不工作

**解决：**
```bash
# 重启开发服务器
Ctrl+C  # 停止
pnpm dev  # 重新启动
```

---

## 开发调试流程 🔄

### 修改核心包代码

```bash
# 1. 修改核心包代码
vim packages/core/src/xxx.ts

# 2. 重新构建核心包
cd packages/core
pnpm build

# 3. 重启示例项目
cd ../../examples/react-example
pnpm dev
```

### 修改 React 包代码

```bash
# 1. 修改 React 包代码
vim packages/react/src/xxx.tsx

# 2. 重新构建 React 包
cd packages/react
pnpm build

# 3. 重启示例项目
cd ../../examples/react-example
pnpm dev
```

### 修改示例项目代码

```bash
# 直接修改示例代码
vim examples/react-example/src/App.tsx

# 保存后自动热更新，无需重启
```

---

## 调试数据流 🌊

### 1. 数据加载

```typescript
// 在 App.tsx 中添加日志
useEffect(() => {
  loadFlowConfig();
}, []);

const loadFlowConfig = async () => {
  const data = { /* ... */ };
  console.log('📦 加载的配置:', data);  // 添加日志
  setConfig(data);
};
```

### 2. 数据变化

```typescript
const handleSave = async (newConfig: FlowConfig) => {
  console.log('💾 保存配置:', newConfig);  // 添加日志
  setConfig(newConfig);
};
```

### 3. 动态选项加载

```typescript
const loadOptions = async (field: FormFieldSchema) => {
  console.log('🔍 加载选项:', field.name);  // 添加日志

  if (field.name === 'approvers') {
    const options = [/* ... */];
    console.log('📋 返回选项:', options);  // 添加日志
    return options;
  }

  return field.options || [];
};
```

---

## 性能调试 ⚡

### 使用 React DevTools

1. 安装 React DevTools 浏览器扩展
2. 打开开发者工具
3. 切换到 "Components" 或 "Profiler" 面板
4. 查看组件渲染情况

### 查看网络请求

1. 打开开发者工具
2. 切换到 "Network" 面板
3. 刷新页面
4. 查看配置文件/API 请求

---

## 测试不同的数据源 📊

### 从 JSON 文件加载

```bash
# 1. 在 public 目录创建配置文件
mkdir -p examples/react-example/public/configs
vim examples/react-example/public/configs/test-flow.json

# 2. 修改 App.tsx
const response = await fetch('/configs/test-flow.json');
const data = await response.json();
setConfig(data);
```

### 模拟 API 加载

```typescript
// 在 App.tsx 中
const loadFlowConfig = async () => {
  // 模拟 API 延迟
  await new Promise(resolve => setTimeout(resolve, 1000));

  const data = { /* ... */ };
  setConfig(data);
};
```

---

## 扩展调试 🔧

### 添加自定义节点类型

```typescript
// 1. 创建自定义节点组件
const CustomNode = ({ data }: any) => (
  <div style={{ padding: 20, border: '2px solid blue' }}>
    <h3>{data.title}</h3>
    <p>这是自定义节点</p>
  </div>
);

// 2. 注册组件
<FlowDesigner
  config={config}
  nodeComponents={{
    'custom': CustomNode
  }}
/>

// 3. 在配置中使用
{
  id: 'node-1',
  type: 'custom',  // 使用自定义类型
  title: '自定义节点'
}
```

### 添加自定义验证器

```typescript
const customValidators = {
  asyncValidate: async (value: any) => {
    console.log('🔍 验证中...', value);
    await new Promise(resolve => setTimeout(resolve, 500));
    console.log('✅ 验证完成');
    return value.length > 5;
  }
};

<FlowDesigner
  config={config}
  validators={customValidators}
/>
```

---

## 构建生产版本 🏗️

```bash
# 构建示例项目
cd examples/react-example
pnpm build

# 预览生产版本
pnpm preview
```

---

## 实用命令速查 📝

```bash
# 安装依赖
pnpm install

# 构建所有包
pnpm build

# 构建核心包
cd packages/core && pnpm build

# 构建 React 包
cd packages/react && pnpm build

# 启动示例项目
cd examples/react-example && pnpm dev

# 构建示例项目
cd examples/react-example && pnpm build

# 查看项目结构
tree -I node_modules -L 3
```

---

## 下一步建议 💡

### 1. 学习源码
- 查看 `packages/core/src/` 了解核心逻辑
- 查看 `packages/react/src/` 了解 React 适配层
- 查看 `examples/react-example/src/App.tsx` 了解使用方式

### 2. 尝试修改
- 修改配置文件，添加新节点
- 添加新的表单字段类型
- 尝试不同的数据加载方式

### 3. 扩展功能
- 集成 ReactFlow 实现可视化拖拽
- 添加节点配置弹窗
- 实现条件分支

---

## 获取帮助 💬

遇到问题？

1. **查看控制台错误** - 打开浏览器开发者工具
2. **查看文档** - 阅读 README.md 和设计文档
3. **查看示例代码** - 参考 `examples/react-example/src/App.tsx`
4. **查看类型定义** - 参考 `packages/core/src/types/index.ts`

---

**祝调试顺利！** 🎉
